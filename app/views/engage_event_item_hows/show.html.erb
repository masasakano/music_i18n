
<h1>EngageEventItemHow: <%= @engage_event_item_how.title_or_alt(langcode: I18n.locale) %></h1>

<hr>
<%= render partial: 'layouts/all_registered_translations', locals: {model: @engage_event_item_how} %>
<hr>

<%= render @engage_event_item_how %>

<div>
    <% if can? :update, @engage_event_item_how %>
  <%= link_to "Edit this EngageEventItemHow", edit_engage_event_item_how_path(@engage_event_item_how) %> |
    <% end %>
  <%= link_to "Back to EngageEventItemHow index", engage_event_item_hows_path %>

    <% if can?(:destroy, @engage_event_item_how) %>
    <%# if !@engage_event_item_how.engage_event_item_how_items.exists? && can?(:destroy, @engage_event_item_how) %>
  <%= button_to "Destroy", @engage_event_item_how, method: :delete %>
    <% end %>
</div>

<% if false %>
<% if can? :read, EventItem %>  <%# should be redundant %>
<hr>

<h2>EventItems for this EngageEventItemHow</h2>

<% items = @engage_event_item_how.event_items.order(:weight, event_ratio: :desc) %>
<%= render partial: 'layouts/event_items_index', locals: {event_items: items, with_event: false} %>
<% end %>

<hr>
  <%= link_to t("layouts.back_to_index"), engage_event_item_hows_path %>
<% end %>  <%# if false %>

