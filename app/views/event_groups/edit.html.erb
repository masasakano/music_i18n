<h1>Editing EventGroup: <%= @event_group.title_or_alt(langcode: I18n.locale) %></h1>

<%= render "form", event_group: @event_group %>

<br>

<%= render partial: 'layouts/link_edit_destroy_index', locals: {record: @event_group, is_to_edit: true, only: :moderator } %>

