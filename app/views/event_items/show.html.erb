<%= render @event_item %>

<div>
    <% if can? :update, @event_item %>
  <%= link_to "Edit this EventItem", edit_event_item_path(@event_item) %> |
    <% end %>
  <%= link_to "Back to EventItems", event_items_path %>

    <% if can? :destroy, @event_item %>
  <%= button_to "Destroy this EventItem", @event_item, method: :delete %>
    <% end %>
</div>
