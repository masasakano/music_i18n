<%# locals: (anchoring: @anchoring) %>

<main class="container">
  <div class="header">
    <h1>Edit Anchoring (for <%= anchoring.anchorable.class.name %>-Url)</h1>
  </div>

  <%= turbo_frame_tag dom_id(anchoring) do %>  <%# "anchoring_123" %>
    <%= render 'form', anchoring: anchoring %>
  <% end %>
</main>

