
<%= render partial: 'layouts/form_print_errors', locals: {model: country} %>

<%= form_with(model: country, local: true) do |form| %>
  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
