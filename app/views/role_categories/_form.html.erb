<%= form_with(model: role_category, local: true) do |form| %>
  <% if role_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(role_category.errors.count, "error") %> prohibited this role_category from being saved:</h2>

      <ul>
        <% role_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :mname %>
    <%= form.text_field :mname %>
  </div>

  <div class="field">
    <%= form.label :note %>
    <%= form.text_area :note %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
