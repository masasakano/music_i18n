<h1>Editing Country Master (pID=<%= @country_master.id %>)</h1>

<%= render 'form', country_master: @country_master %>

<%= link_to 'Show', @country_master %> |
<%= link_to 'Back', country_masters_path %>
