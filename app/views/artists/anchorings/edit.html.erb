<main class="container">

  <div class="header">
    <h1>Edit Anchoring (for Url)</h1>
  </div>

  <%= turbo_frame_tag dom_id(@anchoring) do %>  <%# "anchoring_123" %>
    <%= render 'form', anchoring: @anchoring %>
    <%= link_to sanitize("&larr; Cancel..."),  Rails.application.routes.url_helpers.polymorphic_path(@anchoring.anchorable) %> 
  <% end %>


</main>

