<p id="notice"><%= notice %></p>

<h1><%= t("Artist").pluralize(8, I18n.locale) %></h1>

<%= datagrid_form_with model: @grid, method: :get, url: artists_path %>

<%= paginate(@grid.assets) %>
<%= datagrid_table @grid %>
<%= paginate(@grid.assets) %>

<%= render "layouts/grid_table_tail", klass: Artist %>

<br>

<% if can? :create, Artist %>
 <div class="button_new">
  <%= button_to 'Create New Artist', new_artist_path, method: :get %>
  <%# link_to 'New Artist', new_artist_path %>
 </div>
<% end %>
