
<h1>Channel Platform: <%= @channel_platform.title_or_alt(langcode: I18n.locale) %></h1>

<hr>
<%= render partial: 'layouts/all_registered_translations', locals: {model: @channel_platform} %>
<hr>

<%= render @channel_platform %>

<%= render partial: 'layouts/link_edit_destroy_index', locals: {record: @channel_platform, is_to_edit: false, only: :moderator} %>

